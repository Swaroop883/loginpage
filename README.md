backend system for the login page

run pip install -r requirements.txt to install all the tools

start server by uvicorn main:app --reload

go to http://127.0.0.1:8000/docs -> there use the swagger ui to test the application

comments are written(in own words) at every line to help in understanding the workflow and code
